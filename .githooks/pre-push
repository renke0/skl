#!/bin/sh

echo "Running checks before push..."
./gradlew detektMain detektTest test

if [ $? -ne 0 ]; then
  echo "Checks failed, push aborted"
  exit 1
fi

echo "All checks passed, proceeding with push"
